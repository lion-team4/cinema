# 🎬 Cinema - 실시간 스트리밍 플랫폼

<div align="center">

![Java](https://img.shields.io/badge/Java-21-ED8B00?style=for-the-badge&logo=openjdk&logoColor=white)
![Spring Boot](https://img.shields.io/badge/Spring_Boot-3.5-6DB33F?style=for-the-badge&logo=springboot&logoColor=white)
![Next.js](https://img.shields.io/badge/Next.js-16-000000?style=for-the-badge&logo=nextdotjs&logoColor=white)
![TypeScript](https://img.shields.io/badge/TypeScript-5-3178C6?style=for-the-badge&logo=typescript&logoColor=white)
![MySQL](https://img.shields.io/badge/MySQL-8.0-4479A1?style=for-the-badge&logo=mysql&logoColor=white)
![Docker](https://img.shields.io/badge/Docker-2496ED?style=for-the-badge&logo=docker&logoColor=white)

**실시간 동기화 상영과 WebSocket 기반 채팅을 지원하는 영화 스트리밍 서비스**

[Features](#-주요-기능) • [Architecture](#-시스템-아키텍처) • [Getting Started](#-시작하기) • [API Docs](#-api-문서) • [Deployment](#-배포)

</div>

---

## 📋 프로젝트 소개

Cinema는 **실시간 동기화 상영** 기능을 핵심으로 하는 스트리밍 플랫폼입니다. 
사용자들은 같은 시간에 같은 영상을 함께 시청하며 실시간 채팅으로 소통할 수 있습니다.

### 🎯 핵심 가치
- **실시간 극장 경험**: WebSocket 기반 동기화 재생 및 실시간 채팅
- **자동 상태 관리**: 스케줄러를 통한 상영 상태 자동 전환 (WAITING → PLAYING → ENDING → CLOSED)
- **크리에이터 정산**: 시청 기록 기반 월별 자동 정산 시스템

---

## ✨ 주요 기능

### 👤 사용자 관리
| 기능 | 설명 |
|------|------|
| 회원가입/로그인 | JWT 기반 인증, 이메일/닉네임 중복 확인 |
| 구독 결제 | Toss Payments 빌링키 기반 월간 자동 결제 |
| 마이페이지 | 프로필 관리, 구독 상태 확인 |

### 🎥 콘텐츠 관리
| 기능 | 설명 |
|------|------|
| 영상 업로드 | S3 Presigned URL 직접 업로드 |
| HLS 트랜스코딩 | FFmpeg 기반 자동 인코딩 (다중 해상도) |
| 메타데이터 관리 | 제목, 설명, 태그, 썸네일 관리 |

### 📅 상영 스케줄
| 기능 | 설명 |
|------|------|
| 일정 편성 | 정각 시작, 중복 편성 방지 |
| 자동 상태 전환 | 10초 간격 스케줄러로 상태 자동 관리 |
| 실시간 푸시 | 상태 변경 시 WebSocket으로 즉시 알림 |

### 🎭 실시간 극장
| 기능 | 설명 |
|------|------|
| 동기화 재생 | 서버 기준 재생 위치 동기화 |
| 실시간 채팅 | STOMP 프로토콜 기반 그룹 채팅 |
| 입퇴장 관리 | 시작 10분 전 입장, 종료 10분 후 퇴장 |

### 💰 정산 시스템
| 기능 | 설명 |
|------|------|
| 시청 기록 집계 | 20% 이상 시청 시 유효 조회수 인정 |
| 월별 자동 정산 | Spring Batch 기반 매월 1일 정산 |
| 정산 내역 조회 | 크리에이터별 수익 대시보드 |

---

## 🏗 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────┐
│                            Client                                   │
│                    (Next.js + TypeScript)                           │
└────────────────────────────┬────────────────────────────────────────┘
                             │
              ┌──────────────┼──────────────┐
              │              │              │
              ▼              ▼              ▼
         HTTP/REST      WebSocket      HLS Stream
              │              │              │
              └──────────────┼──────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────────┐
│                       Spring Boot Backend                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌────────────┐ │
│  │   REST API  │  │   WebSocket │  │  Scheduler  │  │   Batch    │ │
│  │  Controller │  │   (STOMP)   │  │  (10s tick) │  │  (Monthly) │ │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └─────┬──────┘ │
│         │                │                │                │       │
│         └────────────────┴────────────────┴────────────────┘       │
│                                   │                                 │
│                          Service Layer                              │
│                                   │                                 │
└───────────────────────────────────┼─────────────────────────────────┘
                                    │
         ┌──────────────────────────┼──────────────────────────┐
         │                          │                          │
         ▼                          ▼                          ▼
   ┌──────────┐              ┌──────────┐              ┌──────────────┐
   │  MySQL   │              │   AWS    │              │     Toss     │
   │   8.0    │              │    S3    │              │   Payments   │
   └──────────┘              └──────────┘              └──────────────┘
```

### 실시간 상태 전환 흐름

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ScheduleStatusScheduler                          │
│                      (매 10초 실행)                                  │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │  상태 변화 감지 (DB 조회)      │
              │  WAITING → PLAYING           │
              │  PLAYING → ENDING            │
              │  ENDING  → CLOSED            │
              └──────────────┬───────────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │  TheaterStateBroadcaster     │
              │  (SimpMessagingTemplate)     │
              └──────────────┬───────────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │  WebSocket Push              │
              │  /topic/theaters/{id}/state  │
              └──────────────┬───────────────┘
                             │
                             ▼
              ┌──────────────────────────────┐
              │  구독 중인 모든 클라이언트     │
              │  실시간 상태 업데이트          │
              └──────────────────────────────┘
```

---

## 🛠 기술 스택

### Frontend
| Category | Technology |
|----------|------------|
| Framework | Next.js 16.1.3 (App Router) |
| Language | TypeScript |
| Styling | Tailwind CSS v4 |
| State | Zustand, React Query |
| Video | hls.js |
| Payment | @tosspayments/payment-widget-sdk |
| WebSocket | @stomp/stompjs, sockjs-client |

### Backend
| Category | Technology |
|----------|------------|
| Framework | Spring Boot 3.5.9 |
| Language | Java 21 |
| Database | MySQL 8.0 (JPA, QueryDSL) |
| Security | Spring Security, JWT |
| WebSocket | Spring WebSocket (STOMP) |
| Batch | Spring Batch |
| Cloud | AWS SDK v2 (S3) |
| Encoding | FFmpeg (Docker) |
| Docs | SpringDoc (Swagger) |

### Infrastructure
| Category | Technology |
|----------|------------|
| Container | Docker, Docker Compose |
| CI/CD | GitHub Actions |
| Deploy | AWS EC2 |
| Storage | AWS S3 |

---

## 🚀 시작하기

### 사전 요구사항
- Java 21
- Node.js 20+
- Docker & Docker Compose
- MySQL 8.0 (또는 Docker)

### 환경 변수 설정

**Backend** (`back/cinema/src/main/resources/application.yaml`)
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cinema
    username: your_username
    password: your_password

cloud:
  aws:
    s3:
      bucket: your-bucket-name
    credentials:
      access-key: your-access-key
      secret-key: your-secret-key

toss:
  client-key: your-toss-client-key
  secret-key: your-toss-secret-key

jwt:
  secret: your-jwt-secret-key
```

**Frontend** (`front/.env.local`)
```env
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws
NEXT_PUBLIC_TOSS_CLIENT_KEY=your-toss-client-key
```

### 로컬 실행

#### Option 1: Docker Compose (권장)
```bash
# 전체 서비스 실행 (MySQL, Backend, Frontend)
docker-compose up -d
```

#### Option 2: 개별 실행

**Backend**
```bash
cd back/cinema
./gradlew bootRun
```

**Frontend**
```bash
cd front
npm install
npm run dev
```

### 접속
- Frontend: http://localhost:3000
- Backend API: http://localhost:8080
- Swagger UI: http://localhost:8080/swagger-ui/index.html

---

## 📖 API 문서

### 주요 엔드포인트

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/auth/signup` | 회원가입 |
| POST | `/auth/login` | 로그인 |
| POST | `/auth/reissue` | 토큰 재발급 |
| GET | `/contents` | 콘텐츠 목록 조회 |
| POST | `/contents` | 콘텐츠 등록 |
| GET | `/schedules` | 스케줄 목록 조회 |
| POST | `/schedules` | 스케줄 생성 |
| POST | `/subscription` | 구독 등록 |
| GET | `/theaters/{scheduleId}/playback` | 재생 정보 조회 |

### WebSocket 토픽

| Topic | Description |
|-------|-------------|
| `/topic/theaters/{scheduleId}/state` | 상영 상태 변경 알림 |
| `/topic/theaters/{scheduleId}/chat` | 실시간 채팅 |

> 📌 상세 API 문서: [API.md](./back/cinema/API.md)

---

## 🚢 배포

### Docker Hub 이미지 빌드 & 푸시
```bash
./build-and-push.sh http://EC2-ip:8080
```

### EC2 배포
```bash
./deploy.sh <EC2_IP_ADDRESS>
```

### GitHub Actions
`.github/workflows/deploy.yml`에서 자동 배포 설정

**필요한 Secrets:**
- `DOCKER_HUB_USERNAME`
- `DOCKER_HUB_TOKEN`
- `EC2_HOST`
- `EC2_USERNAME`
- `EC2_SSH_KEY`
- `NEXT_PUBLIC_API_URL`

---

## 📁 프로젝트 구조

```
cinema/
├── back/cinema/                 # Backend (Spring Boot)
│   ├── src/main/java/com/example/cinema/
│   │   ├── config/              # 설정 (Security, WebSocket, Scheduler)
│   │   ├── controller/          # REST API 컨트롤러
│   │   ├── service/             # 비즈니스 로직
│   │   ├── repository/          # 데이터 접근 계층
│   │   ├── entity/              # JPA 엔티티
│   │   ├── dto/                 # 데이터 전송 객체
│   │   ├── scheduler/           # 배치 작업 (정산, 구독)
│   │   └── infra/               # 외부 연동 (S3, FFmpeg)
│   └── src/main/resources/
│       ├── application.yaml     # 개발 설정
│       └── application-prod.yaml # 운영 설정
│
├── front/                       # Frontend (Next.js)
│   ├── src/
│   │   ├── app/                 # App Router 페이지
│   │   ├── components/          # 재사용 컴포넌트
│   │   ├── lib/                 # 유틸리티, API 클라이언트
│   │   └── types/               # TypeScript 타입 정의
│   └── next.config.ts           # Next.js 설정
│
├── docker-compose.yml           # 로컬 개발 환경
├── build-and-push.sh            # Docker 이미지 빌드 스크립트
├── deploy.sh                    # EC2 배포 스크립트
└── .github/workflows/           # CI/CD 파이프라인
```

---

## 📄 라이선스

This project is licensed under the MIT License.

---

<div align="center">

**Made with ❤️ by Cinema Team**

</div>
